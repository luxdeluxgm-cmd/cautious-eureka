<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingVertical="8dp"
    android:paddingHorizontal="4dp"
    android:backgroundTint="#000000">

    <CheckBox
        android:id="@+id/cb_quest"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="@string/item_quest_default_text"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:buttonTint="#FFFFFF"/>

    <TextView
        android:id="@+id/tv_quest_tag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="10sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:paddingHorizontal="8dp"
        android:paddingVertical="2dp"
        android:background="@drawable/bg_tag_base"
        android:layout_marginEnd="8dp"
        android:visibility="visible"/>

    <TextView
        android:id="@+id/tv_quest_xp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="50 XP"
        android:textColor="#4CAF50"
        android:textStyle="bold"
        android:layout_marginEnd="8dp"/>

    <ImageButton
        android:id="@+id/btn_delete_quest"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@drawable/ic_custom_close"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/item_quest_delete_description"
        app:tint="@android:color/darker_gray"/>

</LinearLayout>